apiVersion: v1
kind: Service
metadata:
  name: vllm-mistral
  namespace: vllm
spec:
  type: NodePort
  selector:
    app: vllm-mistral
  ports:
  - port: 8000
    targetPort: 8000
    nodePort: 30800
